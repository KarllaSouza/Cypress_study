{
  "name": "cypress_project",
  "version": "1.0.0",
  "scripts": {
    "cypress:run": "cypress run --config-file cypress.config.js",
    "start": "npm run cypress:run"
  },
  "dependencies": {
    "cypress": "^13.15.1"
  }
}
